"use strict";
// --- type assertion 
// when we know the type before the typescript machine, it's kind of called assertion
let anything;
anything = 'Hello world';
anything.length; //-- we declared 'as string' cz we know the variable is string. this is called assertion 
